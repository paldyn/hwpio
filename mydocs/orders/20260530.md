# 2026-05-30 — 오늘 할일

## Task #1 — HanPage 데스크톱 앱 (Tauri 1단계)

- **이슈**: [#1](https://github.com/edwardkim/rhwp/issues/1) (M100, v1.0.0) · **브랜치**: `local/task1`
- **상태**: 4 Stage 전체 완료 → **최종 승인 / 이슈 처리 방침 대기**
- **목표**: rhwp-studio(웹) 빌드 산출물 + WASM 엔진을 Tauri v2 셸로 감싼 데스크톱 앱(표시명 HanPage).
  엔진 무수정. **GitHub Pages 무영향(web-inert)** 불변 제약.

### 진행 내역

- [x] Stage 1 — Tauri 스캐폴드 + rhwp-studio dist 로드(PWA 분기, 워크스페이스 격리) · `task_m100_1_stage1.md`
- [x] Stage 2 — 네이티브 열기/저장 dialog + web-inert 브리지(C2/C3) · `task_m100_1_stage2.md`
- [x] Stage 3 — 파일 연결 + 네이티브 메뉴바 + 최근 문서 + 창 상태 · `task_m100_1_stage3.md`
- [x] **작업지시자 지시 — Windows 포함(CI 자동 빌드)** → Stage 4 개정계획 `task_m100_1_impl_v2.md`
- [x] Stage 4 — macOS `.dmg` 로컬 산출 + Windows NSIS CI(`desktop-release.yml`) + Pages 격리 · `task_m100_1_stage4.md`
- [x] 최종 결과 보고서 — `mydocs/report/task_m100_1_report.md`
- [x] 오늘할일 문서 작성(본 파일)
- [ ] (작업지시자) `desktop-v*` 태그 push → CI Windows `.exe`/macOS `.dmg` 산출 확인
- [ ] (작업지시자 승인 후) 이슈 #1 클로즈

### 검증 요약

- 웹 빌드 `tsc` 클린·109 모듈·PWA on·dist 내 `@tauri-apps` **0건** / 데스크톱 빌드 PWA off.
- macOS 번들 `tauri build` exit 0 → `HanPage_0.7.13_aarch64.dmg`(35M) + `HanPage.app`.
- `cargo clippy` 클린, 루트 격리(워크스페이스 멤버 = `rhwp` 1개, tauri 계열 부재).
- 워크플로 YAML 유효 + 트리거 분리(Pages=`main` push / 데스크톱=`desktop-v*` / CLI=`v*`).

### 잔존 (Out of Scope — Phase 2)

- 코드 서명·공증, Linux 인스톨러, universal/Intel mac, 자동 업데이트, 네이티브 rlib 코어 직접 호출.
