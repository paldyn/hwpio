# 2026-05-27 — 오늘 할일

## Task #1145 — RowBreak 표 rowspan 분할 시 첫 행 중복 출력

- 이슈: [#1145](https://github.com/edwardkim/rhwp/issues/1145)
- 마일스톤: M100 / v1.0.0
- 브랜치: `local/task1145`
- 상태: 최종 보고서 승인 대기

### 진행 항목

- [x] 이슈 등록 및 범위 정리
- [x] 작업 브랜치 생성
- [x] 수행 계획서 작성
- [x] 구현 계획 승인
- [x] RowBreak 표 rowspan 분할 조건 수정
- [x] 회귀 테스트 및 SVG 재확인
- [x] WASM 빌드
- [x] 시각 판정
- [x] 최종 보고서 작성

### 재현 샘플

- `samples/2025년 기부·답례품 실적 지자체 보고서_양식.hwpx`

### 핵심 관찰

- HWP/HWPX 공통 조판 경로 이슈로 판단한다.
- 문제 표는 `page_break=RowBreak`이고, 제목 셀이 `row=0`, `row_span=2`이다.
- 현재 조판은 페이지 하단에서 `PartialTable rows=0..1`을 만들고, 다음 페이지에서 `rows=1..3`을 이어 그린다.
- 결과적으로 rowspan 제목 셀이 두 fragment에 모두 포함되어 중복 출력된다.

### Stage 1 결과

- `RowBreak` 표라도 작은 rowspan 제목/라벨 블록에 내부 hard break가 없으면 블록 중간에서 자르지 않도록 수정했다.
- 문제 샘플 page 1에서 `pi=22` partial fragment가 제거되었다.
- 문제 샘플 page 2에서 `pi=22`가 `Table 3x3` 전체로 시작한다.
- 회귀 테스트 `tests/issue_1145.rs` 추가.

검증:

- `cargo fmt --all -- --check`
- `cargo test --test issue_1145 -- --nocapture`
- `cargo test --test issue_1086 -- --nocapture`
- `cargo test --test issue_1105 -- --nocapture`
- `cargo test --lib`
- `docker compose --env-file .env.docker run --rm wasm`

시각 판정:

- 작업지시자 판정 통과

## Task #1139 — `3-09월_교육_통합_2022.hwp` 한컴오피스 시각 정합

| 이슈 | 작업 | 상태 | 메모 |
|------|------|------|------|
| #1139 | `3-09월_교육_통합_2022.hwp` 5쪽 한컴오피스 시각 정합 조사 | Stage 12 시각 검증 통과, 커밋 준비 | 현재 브랜치 `local/task_m100_1139`. `gh` 로그인 계정 `jangster77`은 `edwardkim/rhwp` assignee 지정 권한 제한이 있어 assignee 미지정 상태로 진행한다. Stage 5에서 문27 작은 inline `Picture` 중복 렌더링을 보정했다. PR #1137은 이제 `upstream/devel`에 머지되어 최신 `upstream/devel` 기준으로 task 1139 커밋을 rebase했다. Stage 6에서 한컴 23쪽 vs rhwp 24쪽 문제를 보정했으나 `pi=523`이 9쪽에 남아야 한다는 가정은 작업지시자 한컴오피스 기준과 달랐다. Stage 7에서 `[다른 풀이]` Shape 렌더 누락은 수정했다. Stage 8에서 9쪽 `문8)` 시작을 한컴 기준대로 10쪽 `pi=523`으로 옮기고 전체 23페이지를 유지했으나 작업지시자 시각 판정에서 아직 한컴과 다르다고 재보고했다. Stage 9에서 미주 영역 시작 구분선 `PageItem::EndnoteSeparator`를 추가하고, 대상 HWP5 샘플의 `note_spacing=구분선 아래 2mm`, `raw_unknown=미주 사이 7mm` 의미를 반영했다. `raw_unknown`은 원본 `LINE_SEG` 흐름에 이미 포함되어 있어 중복 가산하지 않았다. rhwp-studio의 상단 `개체 속성` 명령이 `group` 타입 `[다른 풀이]`에도 열리도록 수정했다. Stage 10에서 우클릭 메뉴가 호출하는 `insert:picture-props` 경로도 `group` 타입을 허용하도록 보정하고, `PicturePropsDialog`가 `group`을 도형 탭 구성으로 열도록 수정했다. Stage 11에서 격자 종이 기준 기본값을 HWP 원본 `PageDef` 기준으로 계산해 `가로 9.00mm`, `세로 24.00mm`가 되도록 보정했다. 9쪽 `문5)` 뒤 미주 `vpos` 되감김을 다음 단으로 넘기고, 단 하단에서 다음 단으로 이어지는 `vpos` 되감김 미주 묶음에는 한컴 `미주 사이 7mm`를 좁게 반영했다. 후반 미주 내부 `vpos` 되감기/큰 점프는 lineSeg 위치 span을 우선해 전체 23페이지를 유지했다. Stage 12에서 `hwp3-sample16-hwp5.hwp` 격자 15/20mm 정합 상태의 하단 쪽 테두리/가운데 아래 쪽번호 차이를 보정했다. `PAGE_BORDER_FILL attr=1`, offset 1420HU 기준 하단 테두리는 선 묶음 visual outset을 추가하지 않고 spacing까지만 반영한다. CLI 비교용 `export-svg --show-grid=3mm --grid-origin=15mm,20mm` 옵션도 추가했다. 자동 검증과 WASM 빌드를 완료했고 작업지시자 시각 검증을 통과했다. |
