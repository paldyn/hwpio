# 오늘 할일 - 2026년 4월 23일

## Task #146 — text-align.hwp SVG ↔ 한컴 PDF 렌더링 차이 보정

### 배경

작업지시자가 제공한 `text-align.hwp` 1페이지 문서에 대해 `rhwp export-svg` 출력과 한컴 오피스 PDF 출력(`text-align.pdf`)을 150dpi로 비교한 결과, 페이지 레이아웃 단계부터 시각적 차이가 식별됨.

### 원인 규명 (단계1에서 PDF 좌표 정밀 비교로 확정)

초기 3가지 가설 중 ② Justify 미반영 / ③ Hanging indent 어긋남은 **실제 버그가 아님** 확인 (PDF 좌표 대비 0.04~0.12 pt 오차로 정상 동작). ① 자간×공백 상호작용도 아님.

**진짜 원인**: `is_fullwidth_symbol` 에 Unicode Geometric Shapes 블록(U+25A0-U+25FF, □■▲◆○ 등) 범위 누락 → "□" 이 반각으로 측정되어 후속 글자 전체가 좌측 붕괴.

### 수정

`src/renderer/layout/text_measurement.rs:845` 한 줄 추가 + 단위 테스트 2건 + svg_snapshot golden 1건 업데이트.

### 브랜치

- `local/task146` (from `devel`)

### 문서

- 수행계획서 v2 (범위 축소): `mydocs/plans/task_m100_146_v2.md`
- 구현계획서 v2: `mydocs/plans/task_m100_146_v2_impl.md`
- 최종 결과보고서: `mydocs/report/task_m100_146_report.md`

### 검증 결과

| 항목 | 결과 |
|------|------|
| `cargo test --lib` | 929 passed / 14 failed (14건은 기존 실패, 본 PR 무관) |
| `cargo test --test svg_snapshot` | 3 passed (form-002 golden 업데이트 후) |
| `cargo clippy --lib -- -D warnings` | clean |
| 시각 검증 (`text-align.hwp`) | 제목 "국" x 좌표 PDF 환산과 0.01 px 일치 |
| 스모크 스위프 (`biz_plan`, `exam_kor`, `draw-group`) | 회귀 없음 |

### 진행 상태

- [x] 이슈 확인 · 브랜치 생성 · 현상 조사 · 비교 파이프라인 확보(mutool 설치)
- [x] 수행계획서 v1 작성 · 승인 → v2 로 범위 축소 (원본 ②③ 기각)
- [x] 구현계획서 v1 작성 · 승인 → v2 로 축소
- [x] 단계1: 샘플 편입(`samples/text-align.hwp`) · 비교 파이프라인 고정
- [x] 단계2: `is_fullwidth_symbol` 에 Geometric Shapes 범위 추가 + 단위 테스트
- [x] 단계3: golden 업데이트 + 스모크 스위프 + 최종 결과보고서
