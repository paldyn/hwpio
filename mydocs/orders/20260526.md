# 2026-05-26 오늘할일

| 항목 | 제목 | 상태 | 메모 |
|------|------|------|------|
| Task #1124 | HWPX 다단 세로 구분선 렌더링 누락 개선 | 구현/검증 중 | 작업지시자 제보: `samples/3-11월_실전_통합_2022.hwpx` page 4에서 한컴/HWP 렌더와 달리 좌우 단 사이 세로 구분선이 표시되지 않음. 열린 이슈 검색 결과 동일 증상 없음. 이슈 [#1124](https://github.com/edwardkim/rhwp/issues/1124) 등록. 브랜치 `local/task1124-hwpx-column-line`. HWPX 원문에는 `<hp:colLine type="SOLID" width="0.12 mm" color="#000000"/>`가 있으나 `src/parser/hwpx/section.rs::parse_col_pr()`가 `colPr` 속성만 읽고 자식 `colLine`을 파싱하지 않는 것이 원인. 승인 후 구현했고 HWPX page 4 SVG에 `x=396.853...` 단 구분선이 생성됨을 확인. 작업지시자 지시에 따라 #1122와 #1124를 한 PR로 묶되, 최종 PR 생성은 최종사용자 승인 후 진행. |
