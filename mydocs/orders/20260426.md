# 오늘 할일 - 2026년 4월 26일

## M100 — v1.0.0 조판 엔진

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #338 | Firefox AMO 워닝 해결: strict_min_version 상향 + viewer 번들 보안 워닝 검토 | 완료 | 완료: 03:42, AMO reviewer note 작성 |
| #347 | 표/그림 절대 좌표 계산 오류 수정 (HorzRelTo::Page, HorzAlign::Right, 글뒤로 그림 y 진행, TAC 그림 렌더 누락, InFrontOfText 표 push-down, TAC 그림/표 z-order) | 완료 | exam_eng.hwp p2 우측 박스, p4 우측 Q27/Q28 박스 정상화 |
| #354 | rhwp-firefox/build.mjs CodeQL 경고 (execSync → execFileSync) | 완료 | Code Scanning Alert #17 해소 |
| #359 | 페이지네이션 fit 판정 vs Layout y 진행 정합 (k-water-rfp 311px drift) | 완료 | typeset.rs fit 누적을 total_height 분리 + 단독 항목 페이지 차단 가드. k-water-rfp 73→0, kps-ai 60→4 LAYOUT_OVERFLOW. 1008 lib + 6 svg_snapshot 통과 |
| #377 | API Export 기능 제안 (서버 업로드 가이드 요청) | 완료 (close) | bytes 반환 API (`exportHwp() / exportHwpx()`) 이미 제공. 위키에 [Export API 사용 가이드](https://github.com/edwardkim/rhwp/wiki/Export-API-%EC%82%AC%EC%9A%A9-%EA%B0%80%EC%9D%B4%EB%93%9C) 추가 (서버 업로드 / 로컬 / 진행률 / 인증 / HWPX / Rust native 시나리오 6건). 댓글 안내 + close |
| #375 | 원클릭 실행 스크립트 추가 제안 (@NoteBlockMR) | 완료 (close) | 위키에 [Cloudflared 로 rhwp-studio 외부 HTTPS 접근](https://github.com/edwardkim/rhwp/wiki/Cloudflared-%EB%A1%9C-rhwp-studio-%EC%99%B8%EB%B6%80-HTTPS-%EC%A0%91%EA%B7%BC) 가이드 흡수 (작성자 attribution 보존). zip 직접 흡수는 vite.config 분기 / 포트 변경 / dev server expose 보안 우려로 거절. PR 형태 재제출 시 검토 가능 안내 + close |
| #383 | rhwp-studio PWA 설치 지원 제안 (@dyjung150605) | PR 대기 (OPEN) | vite-plugin-pwa@1.2.0 (peer ≤vite7, `legacy-peer-deps` 우회). 작성자가 4 환경 사전 검증 + CONTRIBUTING.md 의향 확인 절차 준수. 수락 + PR 권유 (manifest scope `/rhwp/` / icon 192·512·maskable / registerType / WASM precache / E2E 회귀 등 7개 점검 항목 안내). enhancement 라벨 추가 |
| #364 | PageLayerTree 생성 API 추가 제안 (@postmelee, #363 후속) | PR 대기 (OPEN) | #363 → PR #385 (v0.7.7) 흡수 흐름의 후속. 수락 + PR 권유. 6개 점검 / 합의 항목 안내 (schema 정리 / schemaVersion 정책 / 변환 정확성 / PR 범위 좁히기 (변환 API 만, renderer 제외) / surface 우선순위 (Rust + WASM 먼저, C ABI 후속) / 회귀 테스트). enhancement 라벨 추가 |
| #394 | rhwp-studio 셀 진입 시 투명선 자동 ON 로직 비활성화 (한컴 출력 정합) | 완료 | input-handler.ts 5 영역 (호출 지점 2 / 메서드 본체 / 상태 변수 3 / 이벤트 핸들러) 주석 처리 — `[Task #394]` 마커로 향후 되돌리기 가능. 1016 lib + 6 svg_snapshot + clippy + Vite 빌드 통과. 작업지시자 시각 판정 통과 |
| #416 | find_bin_data 가드 결함 (Task #195 회귀, 페이지 배경 이미지 잘못 표시) | 완료 | `c.id == bin_data_id` 가드 제거 — `c.id` 는 storage_id, bin_data_id 는 인덱스라 정상 케이스 거짓 실패 (트러블슈팅 #2026-02-17 결함 패턴 재발). 가드 제거 + sparse id 범위 분기 (HWPX 차트 60000+N 보존). 단위 테스트 7개 추가 (1016→1023). hwpspec.hwp 1페이지 페이지 표지 정상 (16×13 → 793×1121). WASM 빌드 통과. 트러블슈팅 문서 회귀 이력 + 추가 교훈 보강. 작업지시자 시각 판정 통과 |
