# 2026-05-31 — 오늘 할일

## 완료/대기 (이월)

- **Task #1** 데스크톱 앱(Tauri): 4 Stage 완료·머지. 잔여 → (작업지시자) `hanpage-desktop-v*` 릴리스 태그 push 시 CI Windows `.exe`/macOS `.dmg` 산출 확인 + 이슈 #1 클로즈(승인 대기).
- **Task #5** 아이콘 macOS 규격화: PR [#6](https://github.com/paldyn/HanPage/pull/6) 머지 · 이슈 #5 클로즈 ✅
- **Task #7** Windows 창 내부 네이티브 메뉴 제거: PR [#8](https://github.com/paldyn/HanPage/pull/8) 머지 · 이슈 #7 클로즈 ✅
  (작업지시자 스크린샷에 아직 보이는 중복 네이티브 메뉴는 #7 수정 **미릴리스 빌드**라 그런 것 — 차기 릴리스에 자동 해소.)
- **Task #4** macOS 코드 서명·공증: Apple Developer 가입 승인 대기 → **일시정지**.

## Task #12 (HanPage) — 창 좁힘 시 문서 중앙정렬·가로 스크롤 버그

- **이슈**: [paldyn/HanPage#12](https://github.com/paldyn/HanPage/issues/12) (M100) · **브랜치**: `local/task12` (origin/main 기준)
- **상태**: **완료 — PR [#14](https://github.com/paldyn/HanPage/pull/14) 머지(`5c0272b2`) · 이슈 #12 클로즈** ✅
- **증상**: 창(또는 브라우저 뷰포트)을 문서 페이지보다 좁히면 페이지가 중앙정렬 안 되고 오른쪽으로 밀려 짤림 + 가로 스크롤 미생성. Windows·macOS 공통. 공유 프런트(`rhwp-studio`)라 GitHub Pages(웹)도 동일 추정.
- **원인(확정)**: 오버레이(자동 숨김) 스크롤바로 가로 오버플로가 있어도 바가 안 보임(스크롤 자체는 동작). 계획서 유력 가설 `Math.max(totalWidth)` 은 페이지>뷰포트에서 no-op 으로 측정 기각.
- **수정**: `editor.css` `#scroll-container` 에 `::-webkit-scrollbar`(WKWebView·WebView2·웹)+Firefox `scrollbar-*` → 스크롤바 상시 노출. CSS-only 1파일 +27줄, `virtual-scroll.ts` 무수정.
- **계획서**: `mydocs/plans/task_m100_hp12.md` · **보고서**: `mydocs/report/task_m100_hp12_report.md`

### 진행 항목

- [x] 이슈 #12 등록 + `local/task12` 분기(origin/main)
- [x] 수행계획서 작성
- [x] Stage 1 — dev 측정으로 **정확 원인 확정**(오버레이 스크롤바) — `working/task_m100_hp12_stage1.md`
- [x] Stage 2 — CSS 최소 수정 + headful 시각검증 + 그리드 회귀 0 + `tsc` 클린 — `working/task_m100_hp12_stage2.md`
- [x] Stage 3 — 최종 보고서 + `main` PR [#14](https://github.com/paldyn/HanPage/pull/14) 생성 (`task12-scrollbar`)
- [x] (작업지시자 승인) PR #14 머지(`5c0272b2`) + 이슈 #12 클로즈 ✅

## Task #13 (HanPage) — 리네임 rhwp-desktop → HanPage-Desktop

- **이슈**: [paldyn/HanPage#13](https://github.com/paldyn/HanPage/issues/13) (M100) · **브랜치**: `local/task13` (origin/main 기준)
- **상태**: **리네임·CI 정합 완료·검증 통과 → main PR 단계** (머지/이슈 클로즈는 후속 승인)
- **계획서**: `mydocs/plans/task_m100_hp13.md` · **보고서**: `mydocs/report/task_m100_hp13_report.md`
- ⚠️ **태그 스킴 변경**: 차기 데스크톱 릴리스는 `desktop-v*`가 아니라 **`hanpage-desktop-v*`** 로 태그해야 트리거됨 (릴리스 표시명 "HanPage Desktop-v{버전}").
- **범위(확정)**: 디렉터리 `rhwp-desktop/`→`HanPage-Desktop/` + npm 패키지명 `hanpage-desktop` + `desktop-release.yml`(경로·트리거 태그 `desktop-v*`→`hanpage-desktop-v*`·릴리스 표시명 `HanPage Desktop-v{버전}`) + `deploy-pages.yml` paths-ignore `HanPage-Desktop/**` + README 참조. **Rust 크레이트 내부 식별자(`rhwp-desktop`/`rhwp_desktop_lib`)는 비노출이라 유지.**
- **결정 포인트(승인 요청)**: ① npm 패키지명 = `hanpage-desktop`(소문자 규약, 추천) ② 릴리스 표시명은 GH식 치환 부재로 **bash 스텝**이 태그 `hanpage-desktop-v{ver}`→ "HanPage Desktop-v{ver}" 산출(워크플로 로직 추가).

### 진행 항목

- [x] 이슈 #13 등록 + `local/task13` 분기(origin/main)
- [x] 수행+구현 계획서 작성 (`plans/task_m100_hp13.md`)
- [x] Stage 1 — 디렉터리 리네임(28파일) + 패키지명 `hanpage-desktop` + README — `working/task_m100_hp13_stage1.md` (`fc628415`)
- [x] Stage 2 — CI 워크플로 갱신(release 태그·경로·표시명 + pages paths-ignore) — `working/task_m100_hp13_stage2.md` (`8475a385`)
- [x] Stage 3 — 최종 보고서 + `main` PR 생성 (`task13-rename`)
- [ ] (작업지시자 승인) PR 머지 + 이슈 #13 클로즈
