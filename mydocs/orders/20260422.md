# 오늘 할일 - 2026년 4월 22일

## Task #229 Fix — 표 셀 압축 narrow glyph 역진 + 셀 채움 복원

### 배경

`samples/hwpx/table-text.hwpx` 셀(예: `"65,063,026,600"`)에서 콤마/마침표가 뒷 숫자와 겹치고, 또한 셀 좌우에 큰 여백 노출. 원인:

1. `21a02ec` (Task #229 후속) 에서 제거된 per-char 50% min_advance 클램프
2. 동 커밋에서 제거된 셀 underflow 양수 자간 확장 분기

### 수정

- 네이티브 3곳 + WASM 2곳 측정 경로에 `letter_spacing + extra_char_spacing < 0.0` 가드 per-char 50% 클램프 복원 → 콤마 역진 해소
- `paragraph_layout.rs` 에 셀 underflow 자간 확장 분기 복원 (자연 폭 > 셀 폭 조건 추가, 3회 수렴 반복) → 셀 폭 채움
- 회귀 테스트 3종 + SVG 스냅샷 하네스 추가

### 브랜치 / 커밋

- 브랜치: `local/task229-fix` (from `devel`)
- 커밋: 2개 (소스 / 문서·테스트·샘플)

### 검증 결과

| 항목 | 결과 |
|------|------|
| `cargo test --release --lib` | 939 passed; 0 failed; 1 ignored |
| `cargo test --test svg_snapshot table_text_page_0` | passed |
| `cargo check --target wasm32-unknown-unknown --lib` | OK |
| `export-svg` 단조성 검사 | regressions = 0 |
| 시각 검증 (`table-text.hwpx`) | 긴 번호 셀 ≈ 셀 inner 채움, 비압축 셀 자연 폭 중앙 정렬 |
| 시각 검증 (`form-002.hwpx`, 10 페이지) | 기존 레이아웃 보존 |

### 문서

- 수행계획서: `mydocs/plans/task_m100_229_fix.md`
- 구현계획서: `mydocs/plans/task_m100_229_fix_impl.md`
- 최종 보고서: `mydocs/working/task_m100_229_fix_report.md`

### 별도 조사 필요 (본 타스크 범위 밖)

- `form_002_page_0` SVG 스냅샷 기존 실패 — 본 수정 적용/미적용 두 상태에서 `actual.svg` MD5 동일(`d34c2c8...`)로 본 수정과 무관. 별도 이슈로 원인 조사 필요.

> ℹ️ 위 두 섹션(#229 Fix, #240)은 기여자 @planet6897 님이 자신의 PR에 포함하여 제출한 기록이다. 아래는 메인테이너의 오늘 처리 기록.

---

## 외부 기여자 PR 3건 처리 (메인테이너 활동)

### PR #234 — @seanshin (Close, 재제출 요청)

- 상태: Close
- 사유: Fork main→upstream devel base 오류, 문서 언어 불일치, 버전 충돌, 커밋 히스토리 등 구조적 문제 다수
- 처리: 정중한 6항목 피드백 + 재제출 가이드 코멘트
- 문서: [mydocs/pr/archives/pr_234_review.md](mydocs/pr/archives/pr_234_review.md), [pr_234_review_impl.md](mydocs/pr/archives/pr_234_review_impl.md)

### PR #235 — @planet6897 (Merge ✅)

- 상태: **MERGED** (`bc9cb8c`)
- 내용: Task #229 표 셀 압축 narrow glyph 역진 + 셀 채움 복원 (위 섹션 참조)
- 처리 경위: 1차 코드 리뷰에서 품질 완벽 판정, 문서 파일명 규칙 6건 지적 → 기여자 전 항목 반영 재제출 → merge
- 검증: clippy 통과, cargo test 983 passed / 0 failed, CI 전 항목 pass
- 영향: Issue #229, #240 자동 close
- 문서: [mydocs/pr/archives/pr_235_report.md](mydocs/pr/archives/pr_235_report.md) 외 2건

### PR #237 — @nameofSEOKWONHONG (Close, 작성자 자진 취소)

- 상태: Close
- 사유: 작성자가 Fork main→upstream devel base 오류를 먼저 인지하고 취소 요청
- 기능 평가: export-text / export-markdown CLI는 환영 기여, 구조만 정리 후 재제출 환영
- 처리: 실수 인지 감사 + 5단계 재제출 가이드 코멘트
- 문서: [mydocs/pr/archives/pr_237_review.md](mydocs/pr/archives/pr_237_review.md), [pr_237_report.md](mydocs/pr/archives/pr_237_report.md)

---

## 인프라·규칙 개선

### `mydocs/pr/` 폴더 체계 신설

- 외부 기여자 PR 검토 기록을 내부 타스크 문서(`plans/`, `working/`, `report/`)와 분리
- 파일명: `pr_{번호}_review.md`, `pr_{번호}_review_impl.md`, `pr_{번호}_report.md`
- 처리 완료 시 `pr/archives/` 로 이동
- 간소화된 4단계 절차 적용 (내부 타스크의 stage 보고서 생략)

### CLAUDE.md / CONTRIBUTING.md 규칙 보완

- 문서 파일명 규칙 명시 (`task_` 접두어 고정, 마일스톤 `m{숫자}` 형식, `_v2/_v3` 접미어, `_fix/_hotfix` 금지)
- 폴더 역할 엄격 표 추가 (`working/` = stage 전용, `report/` = 최종 전용, `orders/` = `yyyymmdd.md` 전용)
- PR 처리 규칙 섹션 신설
- CONTRIBUTING.md에 기여자용 문서 작성 규칙 + `pr/` 폴더는 메인테이너 전용 명시

---

## 트러블슈팅 기록 (PR #235 후속)

### `mydocs/troubleshootings/hwp_letter_spacing_compensation.md` 신설

PR #235에서 기여자 @planet6897 님이 밝혀낸 **HWP 조판 엔진 암묵지 4건**을 재발 방지용으로 기록:

1. 한컴이 `letter_spacing`을 **편집기 폰트 메트릭 차이 보정용**으로 미리 저장 (스펙에 없는 사실)
2. 음수 자간에서 narrow glyph(콤마/마침표) 역진 — 5곳 측정 경로에 `< 0` 가드 per-char 클램프
3. 수렴 반복 3회 — 선형 1회 분배와 클램프 간섭 처리
4. `effective_text_width` 복원 — 정렬 x 시작점 계산

후발 기여자가 `text_measurement.rs` / `paragraph_layout.rs` 수정 시 며칠을 날리지 않도록, 디버깅 신호→의심지점 매핑, 회귀 테스트 6개 위치, 커밋 역사(`8c9b366` → `21a02ec` → PR #235) 기록.

---

## Task #157 — 본문 번호 목록과 표 y 구간 중첩 레이아웃 버그 (진행 중)

### 배경

[Issue #157](https://github.com/edwardkim/rhwp/issues/157) — @SBKIM9704 님 버그 리포트. 주주총회 참석장 HWP의 2페이지에서 본문 번호 목록과 표2가 동일 y 구간에 중첩 배치. SVG/Canvas 공통 발생 → 레이아웃 엔진 문제.

### 초기 확인

- 재현 파일: [samples/hwpx/issue_157.hwpx](samples/hwpx/issue_157.hwpx) (오늘 리포터가 첨부)
- `export-svg --debug-overlay` 실행 결과: **페이지 1에서 `LAYOUT_OVERFLOW` 4건** 발생
  - para 25 (Table), para 26~28 (FullParagraph) 모두 `y=1102.9`, body_area(`bottom=1093.3`) **9.6px 초과**
  - 즉 원래 2페이지로 흘러가야 할 블록들이 1페이지에 잔류하며 중첩

### 진행 예정

- 하이퍼-워터폴 절차에 따라 `local/task157` 브랜치 생성 → 수행계획서 작성 → 승인 요청
- 관련 이슈 #103 (비-TAC wrap=위아래 out-of-flow 레이아웃)과의 관계 정리 필요

## Task #240 — BMP 임베딩 HWP 문서 SVG 렌더 불가 (버그 수정)

### 배경

`bitmap.hwp`, `한셀OLE.hwp` 샘플에서 SVG 내보내기 시 `data:image/bmp` URI로 BMP 원본이 임베딩되어 브라우저에서 이미지 영역이 비어 보임. SVG `<image>` 요소는 `data:image/bmp` 를 표준 지원하지 않음.

### 수정

- `image` crate 추가(`bmp`, `png` features 최소)
- `bmp_bytes_to_png_bytes` 유틸 추가 (`src/renderer/svg.rs`)
- SVG 임베딩 3개 경로(배경 이미지, `render_image_node`, OLE `native_image`)에 BMP→PNG 재인코딩 적용
- 실패 시 원본 BMP 폴백

### 브랜치 / 상태

- 브랜치: `local/task240` (from `local/devel` ← `devel`)
- 상태: 구현·테스트·보고서 완료, **병합 승인 대기 중**

### 검증 결과

| 항목 | 결과 |
|------|------|
| `cargo test --lib` | 941 passed; 0 failed; 1 ignored |
| `bitmap.svg` mime | `data:image/bmp` 0, `data:image/png` 1 |
| `한셀OLE.svg` mime | `data:image/bmp` 0, `data:image/png` 1 |
| 파일 크기 감소 | bitmap.svg 7.6MB → 45KB, 한셀OLE.svg 174KB → 3.7KB |

### 문서

- 이슈 원안: `mydocs/orders/20260422_issue_bmp_svg_render.md`
- 수행계획서: `mydocs/plans/task_bug_240.md`
- 구현계획서: `mydocs/plans/task_bug_240_impl.md`
- 단계별 보고서: `mydocs/working/task_bug_240_stage{1,2,3}.md`
- 최종 보고서: `mydocs/working/task_bug_240_report.md`
